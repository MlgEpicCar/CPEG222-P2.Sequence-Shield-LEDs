# Project 2 - Sequence 8 LEDs on CPEG222 Shield

This program shifts LEDs (D1-D8) either left or right depending on user input. Which LEDs are shifted are decided by the user with the switches (S1-S4). The user can also control the frequency of LED shifts with the CPEG222 Shield's Analong Input.